import 'package:flutter/material.dart';

class addDevice extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: new Text("sdkfjsdkfjh")
    );
  }
}
